# 博客后台管理系统 - 快速启动指南

## 🚀 第一次使用

### 1. 安装依赖

#### 安装后台管理前端依赖
```bash
cd admin-frontend
npm install
```

如果遇到依赖安装问题，可以尝试：
```bash
npm install --legacy-peer-deps
```

#### 后端已有依赖
后端依赖已安装，无需重新安装。如需重新安装：
```bash
cd backend
npm install
```

### 2. 启动服务

#### 方式一：分别启动（推荐用于开发）

**终端 1 - 启动后端 API**
```bash
cd backend
npm run start:dev
```
✅ 后端运行在: http://localhost:3001
✅ API 文档: http://localhost:3001/api

**终端 2 - 启动后台管理前端**
```bash
cd admin-frontend
npm run dev
```
✅ 后台管理系统运行在: http://localhost:3002

#### 方式二：使用脚本启动（需要安装 concurrently）

在项目根目录创建启动脚本：
```bash
# 安装 concurrently
npm install -g concurrently

# 创建启动脚本
cat > start-admin.sh << 'EOF'
#!/bin/bash
concurrently \
  "cd backend && npm run start:dev" \
  "cd admin-frontend && npm run dev"
EOF

chmod +x start-admin.sh
./start-admin.sh
```

### 3. 访问系统

打开浏览器访问：
- **后台管理系统**: http://localhost:3002
- **API 文档**: http://localhost:3001/api

## 📋 功能说明

### 分析概览 (Dashboard)
- 查看网站统计数据
- 访客趋势图
- 热门文章排行

### 文章管理
- 查看所有已发布文章
- 新建/编辑/删除文章
- 搜索和筛选文章
- Markdown 编辑器

### 草稿管理
- 查看所有草稿
- 新建/编辑/删除草稿
- 发布草稿为正式文章
- Markdown 编辑器

## 🔧 常见问题

### Q1: 前端启动后显示空白页面？
**A**: 检查浏览器控制台是否有错误，确保后端 API 已启动。

### Q2: API 请求失败？
**A**: 
1. 确认后端服务运行在 3001 端口
2. 检查 `admin-frontend/.env.development` 中的 API 地址
3. 查看后端控制台是否有错误信息

### Q3: 数据库没有数据？
**A**: 
1. 确保 `backend/blog.db` 文件存在
2. 可以运行种子数据脚本：
```bash
cd backend
npm run seed
```

### Q4: 端口被占用？
**A**: 
- 修改后端端口：编辑 `backend/src/main.ts`
- 修改前端端口：编辑 `admin-frontend/vite.config.ts`

### Q5: TypeScript 类型错误？
**A**: 这些错误在安装依赖后会自动解决。如果仍有问题：
```bash
cd admin-frontend
rm -rf node_modules package-lock.json
npm install
```

## 📝 开发提示

### 修改代码后
- **后端**: 自动热重载（使用 `--watch` 模式）
- **前端**: 自动热更新（Vite HMR）

### 查看 API 文档
访问 http://localhost:3001/api 可以看到所有 API 接口的详细文档。

### 数据库管理
数据库文件位于 `backend/blog.db`，可以使用 SQLite 客户端查看：
- [DB Browser for SQLite](https://sqlitebrowser.org/)
- [SQLite Viewer (VS Code 插件)](https://marketplace.visualstudio.com/items?itemName=alexcvzz.vscode-sqlite)

## 🎨 界面预览

### 分析概览
- 数据卡片展示关键指标
- 访客趋势图（最近7天）
- 热门文章表格

### 文章管理
- 表格展示所有文章
- 搜索框和分类筛选
- 操作按钮：编辑、查看、删除

### 草稿管理
- 表格展示所有草稿
- 操作按钮：编辑、发布、删除

## 🔐 安全提示

当前版本未实现用户认证，建议：
1. 仅在本地开发环境使用
2. 部署到生产环境前添加认证功能
3. 使用防火墙限制访问

## 📚 相关文档

- [完整系统文档](./ADMIN_SYSTEM_README.md)
- [前端 README](./admin-frontend/README.md)
- [后端 README](./backend/README.md)

## 🎯 下一步

1. ✅ 启动系统
2. ✅ 浏览各个功能模块
3. ✅ 创建一篇测试文章
4. ✅ 尝试草稿功能
5. ✅ 查看统计数据

祝使用愉快！🎉
